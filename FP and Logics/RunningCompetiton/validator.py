from userInterface import *
from model import *
from tests import *
from service import *
from prints import *
from repo2 import *
'''
def validCompetitor(competitor):
    if (isNumber(getIdent(competitor)) and not getName(competitor) == "") and isFloat(getAvgSpeed(competitor)): 
        if getAvgSpeed(competitor) > 0 and isFloat(getFatigueRatio(competitor)):
            if  getFatigueRatio(competitor) > 0.01 and getFatigueRatio(competitor) < 0.5:
                return True
    return False
'''