#include "tests.h"
#include <cstring>
/*
void Tests::TestDomain() {
	char title[100], date[100], mirrorImage[100], type[100];
	int numberOfRepetitions = 0;
	strcpy(title, "");
	strcpy(type, "");
	strcpy(date, "");
	strcpy(mirrorImage, "");

	Refurbishment newTask = Refurbishment(title, type, date, mirrorImage);
}
void Tests::TestRepository();
void Tests::TestController();
*/